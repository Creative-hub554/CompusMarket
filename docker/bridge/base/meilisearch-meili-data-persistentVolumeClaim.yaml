#! meilisearch-meili-data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: meilisearch-meili-data
    namespace: docker
    labels:
        com.docker.compose.service: meilisearch
        com.docker.compose.volume: meili_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
