import { describe, it, expect, vi, beforeEach } from "vitest";import {  ConflictException,  BadRequestException,  ForbiddenException,} from "@nestjs/common";import { PostsService } from "./posts.service";import { FollowsService } from "./follows.service";import { StoriesService } from "./stories.service";import { ProfilesService } from "./profiles.service";import { NotificationsService } from "./notifications.service";import { notificationEvents, NOTIFICATION_CREATED } from "../realtime/notification.events";import { PrismaService } from "../prisma/prisma.service";function makePrisma() {  return {    post: {      create: vi.fn(),      findUnique: vi.fn(),      findMany: vi.fn(),      update: vi.fn(),      delete: vi.fn(),    },    reaction: {      findUnique: vi.fn(),      create: vi.fn(),      update: vi.fn(),      delete: vi.fn(),      groupBy: vi.fn(),    },    comment: {      findUnique: vi.fn(),      create: vi.fn(),      delete: vi.fn(),    },    follow: {      findUnique: vi.fn(),      findMany: vi.fn(),      create: vi.fn(),      deleteMany: vi.fn(),      groupBy: vi.fn(),    },    user: {      findUnique: vi.fn(),      findFirst: vi.fn(),      findMany: vi.fn(),      update: vi.fn(),    },    story: {      create: vi.fn(),      findUnique: vi.fn(),      findMany: vi.fn(),      delete: vi.fn(),    },    storyView: {      create: vi.fn(),    },    notification: {      create: vi.fn(),      findMany: vi.fn(),      count: vi.fn(),      updateMany: vi.fn(),    },  };}function makeNotif() {  return { notify: vi.fn(), list: vi.fn(), unreadCount: vi.fn(), markRead: vi.fn() };}const dbPost = {  id: "p1",  content: "hello",  createdAt: new Date("2026-01-01T00:00:00Z"),  author: { id: "u1", name: "Alice", username: "alice", image: null },  media: [{ id: "m1", kind: "IMAGE", url: "x.webp", thumbUrl: null, position: 0 }],  reactions: [{ emoji: "👍", userId: "u2" }],  bookmarks: [],  _count: { comments: 3 },};describe("PostsService", () => {  let prisma: ReturnType<typeof makePrisma>;  let notif: ReturnType<typeof makeNotif>;  let service: PostsService;  beforeEach(() => {    vi.clearAllMocks();    prisma = makePrisma();    notif = makeNotif();    service = new PostsService(prisma as unknown as PrismaService, notif as unknown as NotificationsService);  });  describe("create", () => {    it("creates a text-only post and maps reactions", async () => {      prisma.post.create.mockResolvedValue(dbPost);      const result = await service.create("u1", { content: "hello" });      expect(result.id).toBe("p1");      expect(result.commentCount).toBe(3);      expect(result.reactions).toEqual([{ emoji: "👍", count: 1 }]);      expect(result.viewerReaction).toBeNull();      expect(result.media).toHaveLength(1);    });    it("rejects an empty post", async () => {      await expect(service.create("u1", {})).rejects.toThrow(BadRequestException);    });    it("rejects a video combined with images", async () => {      await expect(        service.create("u1", {          content: "x",          media: [            { url: "v.mp4", kind: "VIDEO" },            { url: "i.webp", kind: "IMAGE" },          ],        })      ).rejects.toThrow(BadRequestException);    });    it("rejects more than one video", async () => {      await expect(        service.create("u1", {          content: "x",          media: [            { url: "a.mp4", kind: "VIDEO" },            { url: "b.mp4", kind: "VIDEO" },          ],        })      ).rejects.toThrow(BadRequestException);    });  });  describe("feed", () => {    it("includes own posts plus followed authors", async () => {      prisma.follow.findMany.mockResolvedValue([{ followingId: "u2" }]);      prisma.post.findMany.mockResolvedValue([dbPost]);      const result = await service.feed("u1");      expect(prisma.post.findMany).toHaveBeenCalledWith(        expect.objectContaining({          where: { authorId: { in: ["u2", "u1"] } },        })      );      expect(result.items).toHaveLength(1);      expect(result.nextCursor).toBeNull();    });  });  describe("react", () => {    it("toggles off when reacting with the same emoji", async () => {      prisma.post.findUnique.mockResolvedValue({ authorId: "u1" });      prisma.reaction.findUnique.mockResolvedValue({ id: "r1", emoji: "👍" });      prisma.reaction.groupBy.mockResolvedValue([]);      const result = await service.react("u2", "p1", "👍");      expect(result.reacted).toBe(false);      expect(prisma.reaction.delete).toHaveBeenCalledWith({ where: { id: "r1" } });      expect(notif.notify).not.toHaveBeenCalled();    });    it("switches emoji and notifies the author", async () => {      prisma.post.findUnique.mockResolvedValue({ authorId: "u1" });      prisma.reaction.findUnique.mockResolvedValue({ id: "r1", emoji: "👎" });      prisma.reaction.groupBy.mockResolvedValue([        { emoji: "👍", _count: { emoji: 1 } },      ]);      const result = await service.react("u2", "p1", "👍");      expect(result.reacted).toBe(true);      expect(prisma.reaction.update).toHaveBeenCalled();      expect(notif.notify).toHaveBeenCalledWith(        expect.objectContaining({ userId: "u1", actorId: "u2", kind: "REACTION" })      );      expect(result.reactions).toEqual([{ emoji: "👍", count: 1 }]);    });  });  describe("comment", () => {    it("notifies the post author", async () => {      prisma.post.findUnique.mockResolvedValue({ authorId: "u1" });      prisma.comment.create.mockResolvedValue({        id: "c1",        content: "nice",        parentId: null,        createdAt: new Date(),        author: { id: "u2", name: "Bob", username: "bob", image: null },      });      await service.comment("u2", "p1", { content: "nice" });      expect(notif.notify).toHaveBeenCalledTimes(1);      expect(notif.notify).toHaveBeenCalledWith(        expect.objectContaining({ userId: "u1", actorId: "u2", kind: "COMMENT" })      );    });    it("rejects a parent comment from another post", async () => {      prisma.post.findUnique.mockResolvedValue({ authorId: "u1" });      prisma.comment.findUnique.mockResolvedValue({ postId: "other-post", authorId: "u3" });      await expect(        service.comment("u2", "p1", { content: "reply", parentId: "cX" })      ).rejects.toThrow(BadRequestException);    });  });  describe("removeComment", () => {    it("forbids deleting someone else's comment", async () => {      prisma.comment.findUnique.mockResolvedValue({ authorId: "u3" });      await expect(service.removeComment("c1", "u2")).rejects.toThrow(ForbiddenException);    });  });});describe("FollowsService", () => {  let prisma: ReturnType<typeof makePrisma>;  let notif: ReturnType<typeof makeNotif>;  let service: FollowsService;  beforeEach(() => {    vi.clearAllMocks();    prisma = makePrisma();    notif = makeNotif();    service = new FollowsService(prisma as unknown as PrismaService, notif as unknown as NotificationsService);  });  it("rejects self-follow", async () => {    await expect(service.follow("u1", "u1")).rejects.toThrow(BadRequestException);  });  it("creates the follow edge and notifies", async () => {    prisma.user.findUnique.mockResolvedValue({ id: "u2" });    prisma.follow.create.mockResolvedValue({});    await service.follow("u1", "u2");    expect(prisma.follow.create).toHaveBeenCalledWith({      data: { followerId: "u1", followingId: "u2" },    });    expect(notif.notify).toHaveBeenCalledWith(      expect.objectContaining({ userId: "u2", actorId: "u1", kind: "FOLLOW" })    );  });  it("ignores duplicates silently", async () => {    prisma.user.findUnique.mockResolvedValue({ id: "u2" });    prisma.follow.create.mockRejectedValue(new Error("P2002"));    await expect(service.follow("u1", "u2")).resolves.toBeUndefined();    expect(notif.notify).not.toHaveBeenCalled();  });  it("suggests popular unfollowed users", async () => {    prisma.follow.findMany.mockResolvedValue([{ followingId: "u2" }]);    prisma.follow.groupBy.mockResolvedValue([      { followingId: "u9", _count: { followingId: 5 } },    ]);    prisma.user.findMany.mockResolvedValue([      { id: "u9", name: "Zara", username: "zara", image: null, bio: null, _count: { followers: 5 } },    ]);    const result = await service.suggestions("u1");    expect(result).toHaveLength(1);    expect(result[0].id).toBe("u9");  });});describe("StoriesService", () => {  let prisma: ReturnType<typeof makePrisma>;  let service: StoriesService;  beforeEach(() => {    vi.clearAllMocks();    prisma = makePrisma();    service = new StoriesService(prisma as unknown as PrismaService);  });  it("expires stories after 24 hours", async () => {    prisma.story.create.mockResolvedValue({ id: "s1" });    await service.create("u1", { mediaUrl: "a.webp", mediaKind: "IMAGE" });    const args = prisma.story.create.mock.calls[0][0];    const expiresAt = args.data.expiresAt as Date;    const diff = expiresAt.getTime() - Date.now();    expect(diff).toBeGreaterThan(24 * 3600 * 1000 - 5000);    expect(diff).toBeLessThan(24 * 3600 * 1000 + 5000);  });  it("groups stories by author with own stories first", async () => {    prisma.follow.findMany.mockResolvedValue([{ followingId: "u2" }]);    prisma.story.findMany.mockResolvedValue([      {        id: "s2",        author: { id: "u2", name: "Bob", username: "bob", image: null },        mediaUrl: "b",        mediaKind: "IMAGE",        caption: null,        createdAt: new Date(),        views: [],      },      {        id: "s1",        author: { id: "u1", name: "Me", username: null, image: null },        mediaUrl: "a",        mediaKind: "IMAGE",        caption: null,        createdAt: new Date(),        views: [{ userId: "u1" }],      },    ]);    const groups = await service.feedForViewer("u1");    expect(groups[0].author.id).toBe("u1");    expect(groups[0].stories[0].viewed).toBe(true);    expect(groups[1].author.id).toBe("u2");    expect(groups[1].allViewed).toBe(false);  });  it("forbids deleting another user's story", async () => {    prisma.story.findUnique.mockResolvedValue({ authorId: "u2" });    await expect(service.remove("s1", "u1")).rejects.toThrow(ForbiddenException);  });});describe("ProfilesService", () => {  let prisma: ReturnType<typeof makePrisma>;  let service: ProfilesService;  beforeEach(() => {    vi.clearAllMocks();    prisma = makePrisma();    service = new ProfilesService(prisma as unknown as PrismaService);  });  it("returns isFollowing=false for anonymous viewers", async () => {    prisma.user.findUnique.mockResolvedValue({      id: "u2",      name: "Bob",      username: "bob",      image: null,      coverImage: null,      bio: null,      createdAt: new Date(),      _count: { posts: 0, followers: 0, following: 0 },    });    const profile = await service.getProfile("u2");    expect(profile.isFollowing).toBe(false);    expect(prisma.follow.findUnique).not.toHaveBeenCalled();  });  it("reports isFollowing=true when the viewer follows", async () => {    prisma.user.findUnique.mockResolvedValue({      id: "u2",      _count: { posts: 0, followers: 0, following: 0 },    });    prisma.follow.findUnique.mockResolvedValue({ followerId: "u1" });    const profile = await service.getProfile("u2", "u1");    expect(profile.isFollowing).toBe(true);  });  it("rejects a taken username", async () => {    prisma.user.findFirst.mockResolvedValue({ id: "someone-else" });    await expect(      service.updateMe("u1", { username: "taken" })    ).rejects.toThrow(ConflictException);  });});describe("NotificationsService", () => {  let prisma: ReturnType<typeof makePrisma>;  let service: NotificationsService;  beforeEach(() => {    vi.clearAllMocks();    prisma = makePrisma();    service = new NotificationsService(prisma as unknown as PrismaService);  });  it("skips self-notifications", async () => {    await service.notify({ userId: "u1", actorId: "u1", kind: "REACTION" });    expect(prisma.notification.create).not.toHaveBeenCalled();  });  it("marks one or all as read", async () => {    await service.markRead("u1", "n1");    expect(prisma.notification.updateMany).toHaveBeenCalledWith({      where: { userId: "u1", id: "n1" },      data: { readAt: expect.any(Date) },    });    await service.markRead("u1");    expect(prisma.notification.updateMany).toHaveBeenLastCalledWith({      where: { userId: "u1", readAt: null },      data: { readAt: expect.any(Date) },    });  });});describe("NotificationsService realtime", () => {
  let prisma: ReturnType<typeof makePrisma>;
  let service: NotificationsService;
  beforeEach(() => {
    vi.clearAllMocks();
    prisma = makePrisma();
    service = new NotificationsService(prisma as unknown as PrismaService);
  });
  it("persists a notification and emits NOTIFICATION_CREATED with the record", async () => {
    const record = { id: "n1", userId: "u2", actorId: "u1", kind: "REACTION", entityId: "p1", message: "x", readAt: null, createdAt: new Date(), actor: { id: "u1", name: "Al", username: "al", image: null } };
    prisma.notification.create.mockResolvedValue(record);
    const emitSpy = vi.spyOn(notificationEvents, "emit");
    await service.notify({ userId: "u2", actorId: "u1", kind: "REACTION", entityId: "p1", message: "x" });
    expect(prisma.notification.create).toHaveBeenCalledTimes(1);
    expect(emitSpy).toHaveBeenCalledWith(NOTIFICATION_CREATED, record);
    emitSpy.mockRestore();
  });
});
