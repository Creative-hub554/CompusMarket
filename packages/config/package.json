{
  "name": "@theo/config",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'No build step'",
    "dev": "echo 'No dev step'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.0.0"
  }
}
